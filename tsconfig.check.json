{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "types": ["node", "w3c-web-hid"],
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noUncheckedIndexedAccess": true,
    "verbatimModuleSyntax": true,
    "isolatedModules": true,
    "skipLibCheck": true,
    "noEmit": true
  },
  "//include": "src/vite-env.d.ts comes along because the checks now reach src/version.ts, which reads the __APP_* globals vite defines at build time. Nothing under tools/check runs through vite, so the declarations are what stand in for it.",
  "include": ["tools/check", "src/vite-env.d.ts"]
}
